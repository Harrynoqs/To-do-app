const listChores = document.getElementById('todoList');
const checkbox = document.querySelector('input[name=checked]');
const choreDesc = document.querySelector('.choreDesc');
const addChore = document.querySelector('input[name=addWorks]');
const clear = document.getElementById('clearAll');
const clears = document.getElementById('recycle');
const addChores = document.getElementById('fa-check');

export {
  listChores, checkbox, choreDesc, addChore, clear, clears, addChores,
};